[project]
name = "vae-torch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pamiq-core",
    "torch>=2.6.0",
]

[tool.uv.sources]
pamiq-core = { path = "../../" }
